/*
package com.edu.testngtest;

import org.junit.Assert;
import org.testng.annotations.BeforeMethod;
import com.edu.classes.testng.TestNGExamples;
import org.testng.annotations.Test;

public class TesNGExamplesTest {
    TesNGExamples te;

    @BeforeMethod
    public void setUp() {
        te = new TesNGExamples();
    }

    @Test
    public void testPositiveNumbers() {
        int actulValue = te.addition(5, 5);
        int expectedValue = 10;
        Assert.assertEquals(expectedValue, actulValue);
    }

    @Test
    public void testNegativeNumbers() {
        int actulValue = te.addition(-5, -6);
        int expectedValue = -11;
        Assert.assertEquals(expectedValue, actulValue);
    }
}
*/
